---
title: Your first workflow
description: Create and run your first duckflux workflow in minutes — a step-by-step guide.
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

## 1. Create the workflow file

Create a file called `hello.flux.yaml` with the following content:

```yaml
flow:
  - type: exec
    run: echo "Hello, duckflux!"
```

That's it — one step, one command. A workflow is just a YAML file with a `flow` field.

---

## 2. Install a runtime

Duckflux workflows are plain YAML files — they need a **runtime** to execute. Pick one:

<Tabs>
  <TabItem label="Go runner">
    See the [Go runner installation guide](/runner-cli/installation/) for setup instructions.
  </TabItem>
  <TabItem label="JavaScript runtime">
    See the [JavaScript runtime installation guide](/javascript-runtime/installation/) for setup instructions.
  </TabItem>
</Tabs>

---

## 3. Run the workflow

Once a runtime is installed, use `quack` to run your workflow:

```sh
quack hello.flux.yaml
```

The runtime reads the file, executes the flow, and prints the output:

```
Hello, duckflux!
```

---

<Aside type="tip">
  Workflows support much more — parallel execution, loops, conditionals, error handling, retry, timeouts, and nested sub-workflows. See [Workflow files](/workflows/) for the full reference.
</Aside>
